name: freeflow
pods:
  - name: main-pod
    public: true
    containers:
      - name: main
        image: main
        env:
          - BETTER_AUTH_SECRET: secrets.BETTER_AUTH_SECRET
          - BOOTSTRAP_TOKEN: secrets.BOOTSTRAP_TOKEN
