name: freeflow
pods:
  - name: main-pod
    public: true
    containers:
      - name: main
        image: main
        env:
          - API_KEY: secrets.API_KEY
          - BETTER_AUTH_SECRET: secrets.BETTER_AUTH_SECRET
          - ADMIN_TOKEN: secrets.ADMIN_TOKEN
